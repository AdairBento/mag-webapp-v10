# husky v9+ hook (sem shebang e sem _/husky.sh)
set -e

echo "ESLint (flat) usando config da raiz em api/"
cd api || exit 1
npx --no-install eslint --config ../eslint.config.cjs "src/**/*.ts" "tests/**/*.ts"